name: Fund Quant Analysis Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    # 每天定时运行，UTC 时间 14:00 (北京……

System: **Error**: Response was cut off due to reaching the maximum allowed length.

### Continuation and Completion of Response

Below is the complete and corrected version of the `fund_quant_pipeline.yml` file, continuing from where the response was cut off. I’ll also summarize the key changes and provide final instructions for using the modified files.

---

### 修改后的 `fund_quant_pipeline.yml` (完整版)

```yaml
name: Fund Quant Analysis Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    # 每天定时运行，例如 UTC 时间 14:00 (北京时间 22:00)
    - cron: '0 14 * * *'

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          
      - name: Install system dependencies
        run: |
          # 安装支持中文的字体
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk
          
      - name: Install Python dependencies
        run: |
          # 安装必要的依赖库
          pip install pandas numpy matplotlib
